# 项目统一依赖文件
# 合并了 core/ 和 android_codegen/ 模块的所有依赖

# ========== 核心依赖 ==========
# ShowDoc 客户端、验证码识别等核心功能
requests>=2.31.0
urllib3>=2.0.0
opencv-python>=4.8.0
numpy>=2.3.0

# ========== 验证码识别依赖 ==========
# 使用 ddddocr，基于深度学习的通用验证码识别库
# 项目地址: https://github.com/sml2h3/ddddocr
# 优点：识别准确率高，支持多种验证码类型，无需额外安装 OCR 程序
ddddocr>=1.4.0

# ========== Android 代码生成器依赖 ==========
# 翻译功能（可选）
# 代码生成器支持自动将中文翻译为英文，支持多种翻译方式

# 方式一：Google 翻译（免费，无需 API 密钥）
# 注意：可能不稳定，受网络环境影响
googletrans==4.0.0rc1

# 方式二：微软翻译 API（推荐，更稳定）
# 无需额外安装依赖（使用 requests，通常已随核心模块安装）
# 配置方式：
#   1. 访问 https://portal.azure.com/ 创建"翻译器"服务
#   2. 设置环境变量 MICROSOFT_TRANSLATOR_KEY（必需）
#   3. 可选：MICROSOFT_TRANSLATOR_REGION（默认 'global'）
#   4. 可选：MICROSOFT_TRANSLATOR_ENDPOINT（默认使用官方端点）
# 免费额度：每月 200 万字符
# 详细配置说明请参考：android_codegen/TRANSLATION.md

# 方式三：仅使用映射表（不安装任何依赖）
# 如果翻译 API 都不可用，会自动使用最小映射表作为兜底

# 优先级：微软翻译 > Google 翻译 > 映射表
# 翻译结果会自动缓存，避免重复翻译相同文本
# ====================================

# ========== 压缩解压工具依赖 ==========
# ZIP 格式增强支持（真正的密码保护，使用 AES 加密）
# 如果未安装，将回退到标准库 zipfile（密码保护有限）
pyzipper>=0.3.6

# 7Z 格式支持（纯 Python 实现）
py7zr>=0.21.0

# RAR 格式支持（需要系统安装 unrar 工具）
# Windows: 需要 unrar.exe 在 PATH 中
# Linux: 需要安装 unrar 包（如 apt-get install unrar）
rarfile>=4.1

# ========== MarkItDown 依赖 ==========
# 将各种资源（URL、文件等）转换为 Markdown
# 支持 http://、https://、file://、data:// 等 URI 格式
markitdown>=0.0.1a24

